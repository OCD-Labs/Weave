# Point Railway's agent service at this file via NIXPACKS_CONFIG_FILE=nixpacks.agent.toml

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/compose.js"